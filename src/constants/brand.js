/**
 * Product name — always English on the public site (not passed through i18n).
 */
export const COMPRESS_PDF_EN = 'CompressPDF.my'

/** `alt` for header/footer logo `<img>` (wordmark is in the PNG). */
export const SITE_LOGO_IMG_ALT = 'CompressPDF.my logo'
